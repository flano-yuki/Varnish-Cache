varnishtest "Simple request with body"

server s1 {
	rxpri

	stream 1 {
		rxreq
		txresp -body "bob"
	} -run
} -start

client c1 -connect ${s1_sock} {
	txpri

	stream 1 {
		txreq
		rxresp
		expect resp.bodylen == 3
	} -run
} -run
