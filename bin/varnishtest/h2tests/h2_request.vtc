varnishtest "H/2 state after sending/receiving preface"

server s1 {
	stream 1 {
		rxreq
		txresp
	} run
} -start

client c1 -connect ${s1_sock} {
	stream 1 {
		txreq
		rxresp
	} run
} -start

server s1 -wait
